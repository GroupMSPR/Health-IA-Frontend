services:
  frontend:
    container_name: healthai_frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development